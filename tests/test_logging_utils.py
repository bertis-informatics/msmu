import logging

from msmu import logging_utils


# def test_setup_logger_sets_level_and_handler():
#     logger = logging_utils.setup_logger(level=logging_utils.LogLevel.DEBUG)

#     assert isinstance(logger, logging.Logger)
#     assert logger.level == logging.DEBUG
#     assert logger.handlers, "logger should have at least one handler attached"
